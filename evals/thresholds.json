{
  "_meta": {
    "purpose": "Quality gates for the RAG eval suite. The CI workflow runs the full eval and asserts every threshold here. If any check fails, the build is red.",
    "set_on": "2026-05-11",
    "set_versus": "evals/results/baseline.json (top_k=5, dense) — the locked Week 2 reference",
    "current_working_point": "hybrid retrieval (BM25+vector+RRF), top_k=12",
    "rationale": "Thresholds are set just under the current working point's measured values, leaving room for judge noise (~±0.02 on aggregates). Tightening these is part of normal iteration; loosening them requires an explicit 'why' note in this file."
  },

  "aggregate": {
    "faithfulness": {
      "min": 0.83,
      "note": "Working point ~0.88. 0.83 catches catastrophic drift; 0.85 is the long-term target."
    },
    "answer_relevance": {
      "min": 0.80,
      "note": "Working point ~0.85."
    },
    "ground_truth_similarity": {
      "min": 0.65,
      "note": "Working point ~0.72. GT-similarity is the noisiest metric (judge phrasing-sensitive); threshold is generous on purpose."
    }
  },

  "failure_modes": {
    "retrieval_miss_max": {
      "value": 9,
      "note": "Working point: 6. Baseline was 10. Floor of 9 means we're not allowed to regress to baseline behavior."
    },
    "none_min": {
      "value": 5,
      "note": "Observed range across 5+ stable runs (same code, same retriever): 6-11. Floor of 5 catches real regression without flaking on judge stochasticity at the 'none' vs 'answer-incomplete' boundary. To be revisited if the regression-vs-last-main check (see below) is extended to cover failure-mode counts.",
      "set_on": "2026-05-13",
      "set_after": "Two consecutive identical CI runs produced none=10 then none=6 — pure judge variance, no code change."
    }
  },

  "stability": {
    "_note": "Stability checks apply only to N-run (scored_n_*.json) eval files. Each question is classified across N runs as 'stable' (std <= 0.10), 'mild-variance' (0.10 < std <= 0.25), or 'bimodal' (std > 0.25). The build fails if too many questions become bimodal — that signals systemic judge instability or a corpus/prompt change that's destabilizing answers.",
    "bimodal_max": {
      "value": 4,
      "note": "Working assumption: ~1-3 bimodal questions across the 41-question set is normal LLM-as-judge noise. Ceiling of 4 catches a clear shift. Tighten as we accumulate more N-run history."
    }
  },

  "canaries": {
    "_note": "These three questions scored 0.00 across all metrics in baseline. Hybrid retrieval fixed Q20 and Q25 reliably. Q06 (frontend/backend/database architecture question) is BIMODAL: the model sometimes refuses ('I don't have enough information') and sometimes synthesizes a strong answer, even with retrieved chunks unchanged. Root cause: borderline grounding decision at temperature > 0, where the strict-grounding system prompt occasionally triggers refusal. With temperature now pinned to 0.0 the behavior is deterministic, but the deterministic outcome depends on retrieval ordering which still has minor non-determinism in hybrid mode. Q06 is therefore kept as an OBSERVABILITY canary (we still see its score in the report) but its threshold is set to 0 — failure of Q06 alone won't block the build. Q20 and Q25 remain HARD canaries.",
    "questions": [
      {"id": "Q06", "min_relevance": 0.0, "soft": true, "note": "Bimodal — observe only, don't gate"},
      {"id": "Q20", "min_relevance": 0.50},
      {"id": "Q25", "min_relevance": 0.40}
    ]
  },

  "regression": {
    "_note": "In addition to absolute thresholds, no aggregate may regress more than this amount from the previous scored run committed to evals/results/last_main.json. Catches gradual drift even when absolute floors are still met.",
    "max_drop_per_metric": 0.05
  }
}
